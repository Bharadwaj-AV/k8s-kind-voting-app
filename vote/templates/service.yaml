apiVersion: v1
kind: Service
metadata:
  name: {{ include "vote.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "vote.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP

